# Core ML
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0

# Progress bars and utilities
tqdm>=4.65.0

# HTTP requests (for Binance API without SDK)
requests>=2.28.0

# Optional: For real trading with Binance SDK
# python-binance>=1.0.17

# Visualization (optional)
# matplotlib>=3.7.0
# seaborn>=0.12.0

# For HPC / distributed training
# NOTE: These are usually provided by the cluster module system
# nccl (loaded via module)
# cudnn (loaded via module)
